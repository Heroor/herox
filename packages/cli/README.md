# @heroor/x

Herox is an Agent CLI for developers.

```bash
npm install -g @heroor/x
herox --help
```

This package currently ships a pure Node.js CLI. Native binary distribution is
planned for a later release.
